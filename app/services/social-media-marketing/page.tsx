import { Services } from './_components/SocialMediaMarketingContent';

export const metadata = {
  title: 'Social Media Marketing Services',
  description: 'Content that gets seen, communities that grow. We build high-converting social content pipelines that turn followers into loyal, engaged audiences.',
};

export default function Page() {
  return <Services />;
}
