import { DisplayNativeAds } from './_components/DisplayNativeAdsContent';

export const metadata = {
  title: 'Display & Native Ads Services',
  description: 'Be everywhere your audience already is. We craft native storytelling and high-impact programmatic display campaigns that build brand equity and convert.',
};

export default function Page() {
  return <DisplayNativeAds />;
}
