import { FAQS } from './_components/FAQSContent';

export const metadata = {
  title: 'FAQs',
  description: 'Answers to common questions about working with Analytics Clouds — our services, pricing, timelines, and reporting process.',
};

export default function Page() {
  return <FAQS />;
}
