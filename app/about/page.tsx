import { AboutUs } from './_components/AboutUsContent';

export const metadata = {
  title: 'About Us',
  description: "We're more than marketers — we're growth partners. See how Analytics Clouds combines data-driven strategy and creative execution to grow your brand.",
};

export default function Page() {
  return <AboutUs />;
}
